version: '3.7'
services:
    ms_server:
        build: 
          context: .
          dockerfile: dockerfile
        ports:  
          - "5000-5005:1433"
        env_file:
          - sqlserver.env
          - sapassword.env
