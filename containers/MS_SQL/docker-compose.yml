version: '3.7'
services:
    ms_server:
        build: 
          context: .
          dockerfile: dockerfile
        ports:  
          - "1433"
        env_file:
          - sqlserver.env
          - sapassword.env
